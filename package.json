{
  "name": "mixapp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev --compatibility-date=2023-06-19 --proxy 5173 -- npm run dev",
    "pages:deploy": "npm run build && wrangler pages publish ./dist"
  },
  "dependencies": {
    "vue": "^3.3.2",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "vite": "^4.3.5",
    "wrangler": "^3.1.0"
  }
}